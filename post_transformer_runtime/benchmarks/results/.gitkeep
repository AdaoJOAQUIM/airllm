# Benchmark results measured on real hardware go here (JSON / markdown).
# Each result JSON embeds a `host` block describing the machine it ran on.
# Do NOT commit hand-written or estimated numbers as if they were measured.
